# Состояние проекта и план

Всё, что нужно для продолжения работы, лежит здесь — в репозитории, а не в переписке.

## Что уже сделано

| Механика | Статус | Где в коде |
|---|---|---|
| Псевдо-3D шоссе (повороты, спуски, подъёмы) | ✅ | `buildTrack()`, `renderRoad()` |
| Трафик с ИИ (перестроения, торможение, столкновения) | ✅ | `spawnCar()`, `updateTraffic()` |
| Нитро: расход, восстановление, баллоны, бонус «вплотную» | ✅ | `updatePlayer()`, `pickups` |
| Урон, авария, перезапуск | ✅ | `updatePlayer()`, `crash()` |
| Закатный фон: горы, город, фонари | ✅ | `drawSky()`, `drawMountains()`, `drawCity()` |
| Адаптивное качество, тач-управление, звук на WebAudio | ✅ | `frame()`, `Sound` |
| Гонка: 3 круга, 4 соперника, позиции | ✅ | `spawnRival()`, `computePlaces()` |
| Отсчёт старта 3-2-1 | ✅ | `countdown` в `updatePlayer()` |
| Финиш: место, время, рекорд в `localStorage` | ✅ | `finishRace()`, `formatTime()` |
| Трамплины: отрыв, полёт, приземление с бонусом | ✅ | `addRamp()`, блок «вертикальная динамика» |
| Дрифт: занос, следы шин, дым, очки → нитро | ✅ | `addSkid()`, `spawnSmoke()`, блок «ДРИФТ» |

## Карта кода (`index.html`, один файл)

```
утилиты и настройки        clamp/lerp/ease, mulberry32, CFG, COLORS, SKY
трасса                     addSegment/addRoad/addRamp/buildTrack/findSegment
мир                        player, cars, pickups, spawnCar, spawnRival, resetWorld
гонка                      computePlaces, formatTime, finishRace
ввод                       keys, readInput, тач-кнопки
звук                       Sound (двигатель, blip-эффекты)
HUD                        el.*, overlayShow, flash, startRun
физика игрока              updatePlayer: разгон, нитро, руль, дрифт, полёт, урон, круги
физика мира                updateTraffic: ИИ, столкновения, обгоны, соперники, баллоны
отрисовка                  project, renderRoad, renderSprites, drawCarRear, drawFlame,
                           drawPickup, drawLamp, drawPlayerCar, drawSpeedLines
цикл                       frame: фиксированный шаг 1/60, адаптивное качество, HUD
тестовый хук               window.__nitro: start/tick/render/state/info/api/finishRace
```

## Как запускать

```bash
make serve      # http://localhost:8000 — игра
make test       # все автотесты (без браузера)
make shots      # скриншоты в headless Chrome (нужен puppeteer, см. ниже)
```

Для скриншотов: `npm i puppeteer` в любую папку и поправить путь в `tests/screenshot-test.js`
(сейчас он смотрит в `/tmp/shot/node_modules/puppeteer`).

### Пуш на GitHub

Репозиторий: <https://github.com/Mihail007p/my-project> (приватный, ветка `main`).

Важно: файл `.git/config` не сохраняется между сессиями, поэтому `origin` иногда теряется.
Восстанавливается одной командой:

```bash
make remote        # вернёт origin = https://github.com/Mihail007p/my-project.git
git push origin main
```

Для пуша нужна авторизация: либо `gh auth login` у себя на машине, либо device-flow
(код с <https://github.com/login/device>) — скрипт лежит в `/home/user/bin/github-device-flow.py`,
если он ещё жив в песочнице; иначе `gh auth login --web` решает ту же задачу.

## Что проверяют тесты

| Файл | Проверок | Про что |
|---|---|---|
| `tests/headless-test.js` | — | загрузка без DOM + 60 секунд симуляции |
| `tests/scenario-test.js` | 25 | удары, обгоны, барьер, трафик, нитро, буст, авария |
| `tests/race-test.js` | 27 | соперники, позиции, круги, финиш, трамплин, полёт над трафиком, дрифт, отсчёт, полная гонка |

Правило: **любая новая механика → проверка в тестах**. Все три файла гоняются в CI.

## Что дальше (по приоритету)

1. **Время суток и погода** — день/ночь, неоновый ночный город, дождь со сменой сцепления.
2. **Контент трасс** — 3-4 разные трассы, выбор перед заездом, у каждой свой сид генератора.
3. **Типы трафика** — грузовики и автобусы: шире, медленнее, больнее; мотоциклы — быстрые, некрупные.
4. **Развилки** — road split с выбором маршрута (короткий/длинный).
5. **Глубже управление** — перегрев нитро, идеальная траектория поворота (бонус за апекс), ручник.
6. **Прогрессия** — сохранение лучших времён по трассам, серия гонок (турнир).

## Известные ограничения

- Баланс соперников подобран «на глаз»: резинка ±30% скорости. Может требовать подкрутки после
  живых заездов — параметры в `CFG` и в `spawnRival()`.
- Урон от трафика одинаков с соперниками; при добавлении грузовиков стоит разнести по типам.
- Следы шин живут до 10 секунд и не сохраняются между заездами (по замыслу).
- Звук синтетический (WebAudio), без сэмплов.

## Соглашения по работе

- Ветка одна — `main`, коммиты по Conventional Commits (`feat:`, `fix:`, `docs:`, `test:`).
- Перед коммитом: `make test` (три набора тестов должны быть зелёными).
- Документацию механик держим в `GAME.md`, план и состояние — в этом файле.
